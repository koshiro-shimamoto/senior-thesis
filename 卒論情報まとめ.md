# 卒論情報まとめ

---

## 実施済み事項

- Remix を使って Solidity のバイトコードの入手方法の確立
- Mythril を用いた脆弱性解析
- EveilM を用いたバイトコード難読化
- Python スクリプトを用いた自動実行(バイトコードレベル)

---

## 土屋先生's ヒント

- **solc に対する難読化ツール**
- **脆弱性検出ツール**（LLM-based＋それ以外）
  - Mythril などがあるらしい
- この脆弱性検出ツールは機械語（EVM bytecode）に対して行う
- Geth は入れてもいいと思う。ー信頼できるところから

---

## 難読化

- **solc とは**
  - スマートコントラクトが記述されている Solidity のコードをバイトコードに変換するコンパイラのこと
- すなわち solc を用いてコンパイルするタイミングで難読化を行う
- 難読化することで動作が変わらんかどうかの確認が必要かも
- solidity コンパイルコマンド: `solc --bin <file>.sol`
- コンパイルした結果バイトコードにずれが生じるのはバイトコード末尾に日時などを記録するメタデータが入るから

これらのツールは ChatGPT に使い方を聞けば大丈夫かも

- BiAn（加道さんが使ってた）
- EVeilM（GitHub）
- HermHD（GitHub） -- edge のお気に入り登録してる
- BOSC（GitHub） -- edge のお気に入り登録してる

## EVeilM

- 動きはする。短いコードに対しては動かない。（一応修正もできる）--きちんと難読化できているのか確認した方がいいかも

難読化手段 — ADD 命令の追加、元の関数名を分かりにくくする、冗長なジャンプ、分岐命令の追加、ジャンプ先アドレスの難読化

- JUMPDEST の大量追加 — CFG (制御フローグラフ) を複雑にするため（obfuscator の 120 行目）
- CFG グラフの複雑化 — JUMPDEST だけでなく、PUSH や到達不能ブロックなどを追加する（138 行目）
- ジャンプ先のアドレス計算化 — プッシュ 10 ですぐにジャンプするのではなく、プッシュ 10 プッシュ 10 ADD でジャンプする感じ（206 行目）
- 関数セレクタ（関数名と引数の型によって作られる4バイトの数字）のアドレスを計算しないと分からないようにする（180 行目）
- EveilM の難読化技法の理解が今途中です

BOSC、HERMHD は論文内で紹介されている。その論文は Gemini に聞けばいいかも — 今聞いてて論文を読んで入手する必要があるかも

BIAN も試さないといけない

---

## Solidity を書ける場所

- Remix
- Zeppelin
- Etherparty
- EthFiddle

---
**デコンパイルツール**
- DiSCo

## Remix デプロイ方法

- Solidity コードをコンパイルし、デプロイ環境は `Prague` を選択、これによりデプロイができる。
- バイトコードはコンパイルの下部にある

---

## 脆弱性検出

スマートコントラクトのセキュリティ分析ツールの例

（追加でほしい場合は加道さんがくれた論文に一覧が載ってる）

**静的解析**
- Slither
- Mythril
- SmartCheck
- Securify
- Vandal-copilotが言うてきた

**動的解析**
- Manticore
- Oyente-copilotも言うてきた

**クラウド分析**
- MythX
- CertiK Skynet
- 

**LLM解析**
- SmartBugBert

---

## myth 解析手法

```
myth analyze "Userから始まる絶対パス(フォルダ移動して相対でもできるかも？)" -t 3(解析の深さ設定用) --execution-timeout 180(ランタイム上限)
--max-depth 30(探索の深さ指定)
--strategy dfs(探索の方法指定)
myth analyze -f "バイトコードファイル"
solc のバージョン指定 --solv 0.8.0 など
```

## バイトと sol で結果が異なる原因

- コンパイラのバージョンが違う、コンパイラの最適化をしていない
- リンクされていないライブラリや import 構造 -- あんまりよくわからん
- コンストラクタとランタイムコードの混同
- Mythril のヒューリスティックの差

- コンパイラの最適化について — 実際の環境では最適化されたコードがほとんどやから最適化したやつで進めた方がいいかも
  - Remix の方は最適化をオフにしている — コンパイルの advanced のところで最適化にできる
  - Mythril で最適化がデフォでオフになっている。オンにするにはそのファイルがある場所へ移動し `--solc-args "--optimize --allow-paths ."` 他にも先に solc でコンパイルしてから解析するのもありかも

---

## データセット利用結果

- 整数オーバフローは死ぬほどでる。多分無視でええ
- 少し解析に失敗するファイルもある — 脆弱性あるのに無いってなるパターン、無いはずの脆弱性を言うてるパターン存在（こっちはほんまにないのか確認の必要あり）
- データセットの注釈の行と解析結果にずれが生じるのは Myth がその脆弱性が起きる箇所、注釈は原因の行を指しているため
- 0.4.15の場合のみ0.4.15が試験用のコンパイラで動かすことができないので0.4.19に変更するようにする
- 0.4.5以下の場合、コンパイラがsolc-selectに存在しないので0.4.19に変更するようにする
- fibonacchi、multouwnws、parity_wallet_1にはコントラクトが二つあるのでどうしたらよいか　A.二個のコントラクトがあるファイルは無視
- unprotectedが動かない A.0.4.15以下はmythrilのコンパイラが対応していない(バイトコードで渡せば動く)

## myth 解析結果

```
C:\Users\koshiro>myth a "mythril/solidity_examples/killbilly.sol" -t 3　//コマンド
C:\Users\koshiro\AppData\Roaming\Python\Python313\site-packages\mythril\plugin\discovery.py:3: UserWarning: pkg_resources is deprecated as an API. See https://setuptools.pypa.io/en/latest/pkg_resources.html. The pkg_resources package is slated for removal as early as 2025-11-30. Refrain from using this package or pin to Setuptools<81.
  import pkg_resources
==== Unprotected Selfdestruct ====//脆弱性名
SWC ID: 106 //脆弱性の識別子
Severity: High  //危険度
Contract: KillBilly //脆弱性を検出したコンストラクト
Function name: commencekilling()  //脆弱性を検出した関数名
PC address: 354 //EVMバイトコード上の命令のオフセット
Estimated Gas Usage: 974 - 1399 //攻撃に必要なガス推定量
Any sender can cause the contract to self-destruct. //脆弱性の説明文
Any sender can trigger execution of the SELFDESTRUCT instruction to destroy this contract account and withdraw its balance to an arbitrary address. Review the transaction trace generated for this issue and make sure that appropriate security controls are in place to prevent unrestricted access.
--------------------
In file: mythril/solidity_examples/killbilly.sol:22 //ソースコードの位置

selfdestruct(msg.sender)

--------------------
Initial State:  //Mythrilがシンボリック実行を始めるときの初期アカウント状態

Account: [CREATOR], balance: 0x0, nonce:0, storage:{} //ここの意味は調べる必要あり
Account: [ATTACKER], balance: 0x0, nonce:0, storage:{}

Transaction Sequence: //どの順序で実行すれば脆弱性にたどり着くかの再現手順

Caller: [CREATOR], calldata: , decoded_data: , value: 0x0
Caller: [ATTACKER], function: killerize(address), txdata: 0x9fa299ccdededededededededededededeadbeefdeadbeefdeadbeefdeadbeefdeadbeef, value: 0x0
Caller: [ATTACKER], function: activatekillability(), txdata: 0x84057065, value: 0x0
Caller: [ATTACKER], function: commencekilling(), txdata: 0x7c11da20, value: 0x0
```

---

## ミーティング（10/15）

- 対象はバイトコード（難読化）
  - リバースエンジニアリングを防ぎたい
- 難読化していないバイトコードと難読化したバイトコードの脆弱性検出の評価
- 難読化手法を複数用意、LLM も複数用意し表を作成したい

> バイトコードに対して難読化してからでもよし、ソースコードに対して難読化してからコンパイルしてもよし

---

## ミーティング（10/29）

- バイトコードの解析には `-f` オプションでいけるかも
- `-t 3` オプションで解析の深さを決めてあげた方がいいかも
- Mythril をいろいろな例で動かして、ちゃんと動いているのか確認する
- 難読化ツールを使ってみる。ダウンロードして実際にコードに適用してみる
- 研究の最終目標が LLM なしになるかも — 複数の難読化手法と解析ツール一つの組み合わせ — 俺が頑張れば LLM までいけるから頑張る

---
## ミーティング（11/12）

- 検出結果が正しいかどうか（偽陽性など）の話 — これに関しては GitHub のファイルを実行することでどうなるか分かるかも
- `eveilm.py` に手を加えたら難読化の手法が選べるようになるかも -- 現在 ChatGPT と会話中
- 脆弱性の GitHub のファイルを Mythril に入れてみる — python でスクリプト組んで自動で実行させれるようにした方がいいかも

バイトと sol で結果が異なる原因を探る必要はあるかも


## ミーティング(11/21)

- バイトコードレベルでの Mythril 実行スクリプトの作成 — その際、コンパイルも自動でできる方が良きかな。`solc-select` ではじめの1行からバージョンを読み取りバージョンを変えてあげる
- GitHub で自分の書いたコードを管理した方が後々いいと思う — auto.py は push 済み。EveilM も git 管理した方がいいかも
- コパイロットで関連研究探し
- まだできていないこと — 難読化技法の確立、GETH を使う

- やらなあかん — コンパイルしてバイトコードを解析する機能（途中）。コントラクトごとの出力でも大丈夫なのか確認、それが無理なら、はじめに2行を削除して対応
- solcでコンパイラを選択してバイトコードを解析することに成功(一つのファイルに複数のコントラクトがあればまずいかも)
- やらなあかんーバイトコードの出力結果が合っているのかの確認、関連研究の確認、gitに挙げる- バイトコードの出力結果は合うているでしょう。gitも完了

## ミーティング(12/03)
- AI信じすぎない
- エチルナー難読化したコードが元のコードと同じ性能であるのかの確認用(加道さんがスラックに挙げてくれてるツールでも確認できる)
- deepseek coder使ってみるといいかも
- 難読化全部のせを適用し、mythrilの結果が変わらないのかの確認
- 難読化検定完了後LLMベースの解析ツールを探してみる
　　　　　　　　また、難読化手法を増やす方向で行くのか土屋先生と相談かも
- 関連研究探しは最後にするかも
